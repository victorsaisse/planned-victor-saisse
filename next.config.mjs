/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "tuogqtvpasmyytgswncm.supabase.co",
      },
    ],
  },
};

export default nextConfig;
